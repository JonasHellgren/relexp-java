package core.gridrl;

/**
 * Represents the variables of the lunar lander environment.
 * This record contains the height (y) and speed (spd) of the lander.
 */
public record VariablesGrid(
        int x, int y
) {
}
